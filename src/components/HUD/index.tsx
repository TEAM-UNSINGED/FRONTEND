import React from 'react';

import { Container, Background } from './styles';

const HUD: React.FC = () => {
  return (
    <Container>
      <Background/>
    </Container>
  );
};

export default HUD;
